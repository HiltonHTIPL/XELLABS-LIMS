# Claude Code
CLAUDE.md
.claude/

# Startup / reference docs
start-commands.txt

# Local change tracking (cleared after every push — never commit)
pending-changes.md

# Bug backlog (local only — worked from directly, never committed)
needtofixbugs.md

# Local scratch / handoff notes — not project source
qa-admin-handoff.html
serversideerrorfixes.md
learning.md

# Claude project map — auto-generated, never committed
project-map.md

# Celery runtime files
celerybeat-schedule
celerybeat.pid

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
.installed.cfg
xellabs-backend/lib/
xellabs-backend/lib64/

# Virtual environment
xellabs-backend/venv/
venv/
env/
.env/

# Django secrets — never commit real .env files
xellabs-backend/.env
*.env
# BUT always commit .env.example files
!*.env.example

# Django generated
xellabs-backend/db.sqlite3
xellabs-backend/media/
xellabs-backend/staticfiles/

# Logs
*.log
logs/

# Docker
.dockerignore

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Senaite rebrand backups
senaite-rebrand/*.bak

# Temp scripts
xellabs-backend/reset_inventory.py

# Backup script and backup output (server-side only — not tracked in git)
backup.sh
xellabs-backups/
userscredential.txt

# Superset Data (Do not commit)
*.db
imm_data/
superset_home/
SENAITE_Module_Field_Workflow_Spec.xlsx

# env templates (user preference: never commit)
.env.example
**/.env.example

# scripts/templates with embedded credentials (kept locally only)
senaite-rebrand/rebrand.py
senaite-rebrand/fix_title_full2.py

# Testing (local-only, per user request)
e2e-playwright/
xellabs-backend/unit_tests/
sample-imports/
TEST-REPORT.md
