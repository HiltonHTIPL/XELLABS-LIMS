# Xellabs LIMS — Work Progress Log

> Entries older than 7 days are automatically removed.
> Format: date → task list. Ask Claude "what did we do today?" to get a summary.

---

## 2026-07-24

- **Chain of Custody — brought up to formal-COC-document standard**: added Collection Date/Collector/Source Location + Required Analyses to the Sample Information card; **signature-gated Log Custody Event** (reuses the existing generic `workflow.ElectronicSignature` "sign" endpoint already proven by Approvals — password re-verified server-side, not just a name picked from a dropdown); Seal Status now **required** when action is Received/Released-for-Analysis; new **Print COC Document** action (formal printable form: sample id/source/collection time/collector/required analyses/full signed custody log). Extended `ChainOfCustody.ACTION` with two real missing lifecycle steps — `batched` ("Added to Batch") and `completed` ("Sample Completed", auto-logged on publish) — migrations `lims/0032`/`0033`.
- **Root-caused and fixed a real silent-failure bug** in every new custody auto-log call (`receiveSample`/`publishSample`/`addAnalysesToWorksheet`/`assignSamplesToBatch`): (1) the sample-resolution lookup only filtered Django's `sample_id` field, but every caller passes the **SENAITE AR id** (e.g. "SO-0024") — added `senaite_ar_id`/`barcode` to `SampleViewSet.filterset_fields` and made the lookup try all three fields in order, mirroring the pattern already proven correct in `_resolve_canonical_sample_id`; (2) the logging calls were unawaited fire-and-forget inside Server Actions — awaited all of them so they're guaranteed to complete before the action returns. Verified via direct view invocation (`RequestFactory`) and a full manual lifecycle run.
- **Full end-to-end lifecycle verification**: created a real sample (SO-0026, Liji Farms, Soil, Soil Calcium & Magnesium + Soil Available Minerals) and drove it through Receive → Sent for Analysis (Worksheet WS-031) → Submit (real interim-field calculation, SOILMIN=68.0, CAMG=1430.0) → Verify → Publish/Complete, confirming the full custody trail (Received/Analysed/Completed) plus per-analysis Result Submitted/Verified populate correctly.
- **SENAITE `SENAITE_SITE_PATH` double-prefix bug (404 on worksheet/any write) — root-caused and fixed**: the whitelabel-proxy `SENAITE_URL` (`:8096`, no path) hit a `|| '/senaite'` fallback in `app/lib/senaite.ts` and a `settings.DEBUG`-based ternary in `core/senaite_service.py`, both force-prepending `/senaite` — the proxy's own Zope `VirtualHostBase` rewrite already inserts it, so every write request got double-prefixed into a nonexistent `senaite/senaite/...` path. Fixed both to derive the path straight from `SENAITE_URL` with no forced fallback (matching `SENAITE_RAW_PATH`'s already-correct pattern from an earlier, never-committed server-only fix — patched surgically to avoid re-clobbering that divergence). **Wrong turn corrected mid-fix**: first attempt wrongly repointed `SENAITE_URL` at the direct `senaite:8080` container, not realizing `:8096` and `:8080` are two *different* SENAITE instances on separate Compose networks that happen to share the service name `senaite` — `:8096` is the one with the real production data (confirmed live: real samples, count 50+). Reverted; real fix was code-only. Same fix applied to both local and the QA server (`34.30.6.247`), tracked in that server's `Prodfix.txt`.
- **Redis → Valkey** (local + QA server): Redis 7.4.9 relicensed off BSD to non-OSI RSALv2/SSPLv1 in 2024 — swapped to Valkey 8.1.9 (BSD-3-Clause, wire-compatible fork), one line in `docker-compose.yml`, zero app changes, verified clean Celery/Django reconnect both places.
- **F.2.6 Open Source Disclosure** — audited every direct frontend/backend dependency's actual installed license (not from memory); found the Redis gap above and an SENAITE-core AGPL-3.0 network-copyleft disclosure (OSI-approved, not a gap, but modified-source-availability flagged for legal). Delivered as an artifact + real `.docx` (hand-built OOXML, no python-docx dependency) for both local and the QA server's state.
- Fixed: per-analysis "Analysis Request ID" column (was showing the parent sample id, now the analysis's own `getRequestID`), Sample Detail barcode print (real Code128 popup matching Storage Manager's label-print pattern instead of `window.print()` on the whole page), New Specification drawer's missing scrollbar class, Analysis Profile create (was a stale Server Action bundle after editing action files without restarting), Batch page 404 (same stale-bundle class), Sample status not syncing to Batch view.
- Dedicated SENAITE-native sample Audit Trail page (`/dashboard/samples/[id]/audit-trail`) — same searchable/filterable `AuditEventsTable` the Django-mirrored Sample Detail already used, fed by this sample's Django-bridged events. Pushed to `hephzibah/staging-development` as `d9b364f`; a teammate's own follow-up commit (frontend port hardening) already merged on top, confirmed live on the QA server.

## 2026-07-23

- **E-signature-gated Approvals flow** (verified → approve with a real password signature → Completed):
  - Backend: `Approval`/`ElectronicSignature` GenericFK made nullable + SENAITE-sample fields (senaite_uid/sample_id/client_name/title); `decide` now truly verifies `request.user.check_password` (wrong → 400 + SecurityEvent, no state change) and records an ElectronicSignature for both approve & reject; new idempotent `request-sample` endpoint. Migration `workflow/0002`, applied to tenant schemas.
  - Frontend: `verifySample` opens the approval on success; `decideApproval` chains the SENAITE publish (verified→published) on a signed approve; sample-detail direct Publish button replaced with an "Awaiting Approval" indicator; approvals table shows real Sample/Client/Analysis/Requester; `mapSenaiteState('published')` now reads "Completed".
  - Verified via DRF APIRequestFactory in the tenant schema (request 201 + idempotent; wrong-password 400/blocked; correct-password approved+signed+uid-returned; already-decided 400). `manage.py check` + `tsc --noEmit` clean.
  - Gotcha: this tenant's `admin` password is NOT "admin" — used a throwaway reviewer user for the happy-path test.
- **Approvals page redesigned** to match the supplied dashboard mockup (honest-data approach): header actions, 5 stat tiles (Pending/My Queue/Approved-this-month/Rejected-this-month/Total), filter bar (Status/Assigned/Date), DataTable (Priority/Sample/Client/Step/Requested-By/Waiting-Since + Review), Recent History + My Queue panels, and an inline Approval Details panel with a SENAITE-state-derived Workflow Progress stepper + inline digital-signature Approve/Reject (replaces the old popup modal). Priority snapshotted onto the Approval at verify-time (`Approval.priority`, migration `workflow/0003`) to avoid an N+1 fetch. Un-backed mockup bits (Due Date/Overdue, Module, multi-step QA/Final) omitted rather than faked. `tsc` clean.
  - **Role-privilege matrix (9 roles × 24 activities)**: expanded `User.role` to 9 lab roles (client/lab_clerk/sampler/analyst/verifier/lab_manager/publisher/manager/admin) and replaced the rank-based permission model with an explicit, **non-hierarchical** `ACTIVITY_ROLES` matrix + `requires(activity)` factory in `core/permissions.py` (single source of truth). Rewired every matrix-relevant viewset/action across core/lims/workflow/audittrail/reporting; kept the generic "…OrAbove" classes (rebuilt on new roles) for non-matrix CRUD. Migration `core/0011` alters role choices + remaps existing users (reviewer→verifier, receptionist→lab_clerk). Frontend: `roles.ts`/badges (9 roles) + Sidebar/adminNav module visibility recomputed from the matrix (Users tile admin-only), `mapSenaiteRole` extended. Verified requires() enforces the non-hierarchical cases (LabManager blocked from enter_results/perform_qc; Verifier blocked from approve). Also fixed the Administration Users/Groups tables' missing horizontal scrollbar (`overflow:hidden`→`overflowX:auto` + `minWidth`). Gaps flagged: Collect Sample + Publish Results have no dedicated Django endpoint yet; live sessions need a re-login to pick up remapped roles.
  - **Trigger fix**: the approval is now created by **reconcile-on-Approvals-load** (fetch samples at SENAITE `verified` → create any missing approval), NOT by a verifySample hook — because real verification happens on the **worksheet** (verify analyses), which never calls sample-level verifySample. One path-agnostic mechanism covers worksheet/sample-detail/bulk/native verify. `requested_by` made nullable (migration `workflow/0004`) so system-detected approvals show "System (verified)". Reconcile skips any UID that already has an approval in any status (no resurrection of rejected/approved). Verified via APIRequestFactory (create/idempotent/skip-rejected); tsc + Django check clean.
  - **Synchronous sample-status sync** (frontend→Django→SENAITE→Django→UI): new `refresh_one_sample_status` + `POST /api/lims/samples/refresh-status/` (reads live SENAITE review_state → maps → updates Django `Sample.status` forward-only, matched by `senaite_uid`; accepts an analysis uid too, resolving its parent AR). Wired `refreshSampleStatus()` (new leaf action `app/actions/sample-status.ts`) into receive / worksheet result-submit / worksheet-verify / sample-verify / approval-publish, revalidating the dashboard + overview — so status changes reflect immediately, no 5-min lag. Also fixed the periodic `pull_samples_and_results` matching (was keyed on the usually-empty SENAITE `ClientSampleID` → skipped everything, synced 0; now matches `senaite_uid`/`senaite_ar_id` → synced 14) and kept Celery Beat as a backstop (it also mirrors results + catches out-of-app changes). Verified live: refresh flipped Test-0061 received→published; dashboard "Completed" KPI (status='published') now counts it. tsc clean.
  - Environment note: SENAITE data is fully intact (63 clients, 100 samples, 11 sample types, 29 services, 16 worksheets). An earlier same-day claim that SENAITE was "empty" was a MISTAKE — it came from querying the SENAITE REST API with `admin:admin` (per CLAUDE.md) instead of the real `SENAITE_ADMIN_PASS` in `.env`; SENAITE accepted the bad login as anonymous (HTTP 200, not 401), and anonymous can't see content, so every count returned 0. Tell-tale: the users endpoint returned only "Anonymous User". **When querying SENAITE directly, always authenticate with `SENAITE_ADMIN_PASS` from `.env`, never `admin/admin`.** The Approvals queue is empty only because 0 samples are at `verified` yet; 13 are at `to_be_verified` — verifying any one will create an approval.

## 2026-07-20

- **Completed all 6 deferred worksheet backlog items** ("do all"):
  1. **Blank/Control QC** — new Administration modules: **Suppliers**, **Reference Definitions** (with expected result/min/max grid + Blank/Hazardous), **Reference Samples** (picks a definition → copies its results; ExpiryDate required). Wired the worksheet detail's "Add Blank/Control QC" picker to the baked `@@add-worksheet-reference` view. No custom Zope views / no rebuild needed for the 3 types (Supplier is DX now; RefDef/RefSample write via restapi — RefSample via create-then-PATCH to dodge a response-serializer 500). See CLAUDE.md §16e.
  2. **Reviewer test user** — documented in `docs/worksheet-testing.md` (self-verification off → enable it for testing or add a reviewer user).
  3. **Analyst picker** — worksheet analyst is now a dropdown of lab members via new `@@lab-analysts` SENAITE view (empty in dev until real lab users exist; falls back to Unassigned + preserves current value).
  4. **Clear Instrument/Method to None** — `@@update-worksheet` view now clears via `getField().set(ws, None)` (setInstrument("") crashed in api.get_object); frontend always sends the fields.
  5. **Instrument results import** — worksheet detail "Import results" (CSV, matches Export format by Position) pre-fills result inputs → "Submit all". Frontend-only.
  6. **Retired orphaned Django worksheet code** — deleted `LabWorksheetsShell.tsx`, `LabWorksheetDetail.tsx`, `app/actions/django-worksheets.ts`.
- SENAITE image rebuilt (2 new views + 1 changed) and recreated. Verified end-to-end in a real browser (Playwright): supplier → definition → sample → QC picker → Control row added. `tsc` clean, no console errors. Test records deactivated (one blank test worksheet WS-014 has no clean REST delete — harmless dev leftover).

<!-- 2026-07-17 and 2026-07-16 sections pruned (older than the 7-day rolling window) -->

- Coordinated with a second concurrently-running Claude Code instance touching the same repo — confirmed no file overlap before proceeding; merged in its Test-model-removal/Calculations work from `hephzibah/staging-development` via stash/pull/merge/pop, resolving the resulting conflicts (Test model, NewSampleShell pricing, this log, CLAUDE.md, migration numbering).

## 2026-07-22
- Built reusable DataTable on @liji-table 0.0.8-beta.0 (sort/pin/reorder/resize/autosize/persistence, SSR hydration guard, universality opt-outs). Mapped dashboard Recent Samples via RecentSamplesTable wrapper.
- Wrote LIJI-TABLE-FEEDBACK.md for the library author.
- Pushed everything as-is (96 files, incl. concurrent session work + full table rollout) to hephzibah staging-development as commit 799d079 (--no-verify, per user request).
